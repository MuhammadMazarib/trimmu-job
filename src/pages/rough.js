/*
    const filterNames=e=>{
      const search=e.target.value.toLowerCase();
      const filteredNames=data.jobs.filter(names=>names.title.toLowerCase().includes(search))
      setNames(filteredNames);



      ///const [names,setNames]=useState(data.jobs);

      //{data.jobs.map(createCard)};
    }*/


